 { 
   "posts": [
      {%    for post in posts  %}
            {
            "title": "{{ post.title }}",
            "created":"{{ post.created| date('Y-m-d H:i:s') }}",
            "contents": "{{ post.contents | raw}}",
            "permalink": "http://frankensteinosaur.us/post/{{ post.path }}"
            }
            {% if not loop.last %},{% endif %}
      {% endfor %}
            ]
}
 
