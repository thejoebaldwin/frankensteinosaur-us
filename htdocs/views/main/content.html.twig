{% extends "base.html.twig" %}
{% block content %}
 <div id="content" class="content">
        <h1>{{ post.title }}</h1>
        {{ html|raw }}
        tags:{% for tag in post.tags  %}<a href='/tag/{{tag}}'>{{tag}}</a>{% if not loop.last %},{% endif %}{% endfor %}
 </div> 
{% endblock %}
